import React from 'react';
import { Bell } from 'lucide-react';

const Header = () => {
  return (
    <header className="topbar" style={{ marginLeft: '220px', justifyContent: 'flex-end', height: '0', padding: '0', border: 'none' }}>
    </header>
  );
};

export default Header;
